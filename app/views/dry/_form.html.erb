<%= form_for(@day) do |f| %>

  <%= f.hidden_field :date %>

  <div class="field">
    <%= f.label :text, @day.date %><br>
    <%= f.text_area :text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
