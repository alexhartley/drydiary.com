<%= form_for(day, remote: true) do |f| %>

  <%= f.hidden_field :date %>

  <div class="field">
    <%= f.label :text, day.date %><br>
    <%= f.text_area :text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
